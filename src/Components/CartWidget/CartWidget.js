import React from 'react'
import { BsBag } from "react-icons/bs";

const CartWidget = () => {
    return (
        <div>
            <button type="button ">
                <BsBag/>
            </button>

        </div>
    )
}

export default CartWidget